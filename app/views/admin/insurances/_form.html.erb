<%= form_with(model: admin_insurance) do |form| %>
  <% if admin_insurance.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(admin_insurance.errors.count, "error") %> prohibited this admin_insurance from being saved:</h2>

      <ul>
        <% admin_insurance.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :email, style: "display: block" %>
    <%= form.email_field :email %>
  </div>

  <div>
    <%= form.label :contact_name, style: "display: block" %>
    <%= form.text_field :contact_name %>
  </div>

  <div>
    <%= form.label :color, style: "display: block" %>
    <%= form.color_field :color %>
  </div>

  <div>
    <%= form.collection_check_boxes :user_ids, User.all, :id, :email %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
